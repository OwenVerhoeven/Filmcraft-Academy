# ADR 0001: React/Vite/TypeScript

Use React + Vite with strict TypeScript. It supports a responsive PWA, fast local development, code-split views and testable domain logic without paid infrastructure. Revisit only if native device APIs become a requirement.
