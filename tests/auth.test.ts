import{describe,expect,it}from'vitest';import{readFileSync}from'node:fs';
describe('account security contract',()=>{const source=readFileSync('src/auth.tsx','utf8');it('contains both account identities',()=>{expect(source).toContain('SinbodWayne');expect(source).toContain('KyanWayne')});it('requires cloud login and persists the revocable session across reloads',()=>{expect(source).toContain('cloudLogin(username, password)');expect(source).toContain('localStorage.setItem(TOKEN_KEY');expect(source).not.toContain('sessionStorage')});it('uses per-account storage keys',()=>{const storage=readFileSync('src/lib/storage.ts','utf8');expect(storage).toContain('account:${account}')})});
