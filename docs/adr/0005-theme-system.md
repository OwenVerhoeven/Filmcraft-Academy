# ADR 0005: Tokenized FilmCraft theme

Ship one polished theme implemented with CSS custom properties and component variants. Color, type, spacing, radius, elevation and motion tokens are centralized so future themes do not require component rewrites. Reduced-motion behavior is mandatory.
