import{describe,expect,it}from'vitest';import{readFileSync}from'node:fs';
describe('account security contract',()=>{const source=readFileSync('src/auth.tsx','utf8');it('contains both account identities',()=>{expect(source).toContain('SinbodWayne');expect(source).toContain('KyanWayne')});it('stores only fixed-length client fallback hashes',()=>{expect(source).toMatch(/[a-f0-9]{64}/);expect(source).not.toMatch(/password\s*:\s*["'][^"']+["']/i)});it('uses per-account storage keys',()=>{const storage=readFileSync('src/lib/storage.ts','utf8');expect(storage).toContain('account:${account}')})});
