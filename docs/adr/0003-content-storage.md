# ADR 0003: Versioned canonical content

Curriculum is deterministic TypeScript data validated by Zod at build/test time. Every skill carries version and source metadata. Progress references stable IDs, never array position or display title.
